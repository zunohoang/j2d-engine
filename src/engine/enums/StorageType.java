package engine.enums;

/**
 * Enum to define the type of storage for data.
 * This can be used to specify where the data is stored,
 * such as in a local JSON file, in the cloud, or in memory cache.
 */
public enum StorageType {
    LOCAL_JSON,
    CLOUD,
    MEMORY_CACHE
}
